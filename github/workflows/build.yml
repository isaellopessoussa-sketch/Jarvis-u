name: Construir APK do Jarvis

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Pegar código do repositório
        uses: actions/checkout@v3

      - name: Configurar Node.js
        uses: actions/setup-node@v3
        with:
          node-size: '16'
          cache: 'npm'

      - name: Instalar o Java (JDK 17)
        uses: actions/setup-java@v3
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Preparar Diretórios e Instalar Dependências
        run: |
          mkdir -p www
          cp index.html www/index.html
          npm install

      - name: Sincronizar Projeto Android (Capacitor)
        run: |
          npx cap android add || true
          npx cap sync

      - name: Compilar o APK de Verdade
        run: |
          cd android
          ./gradlew assembleDebug

      - name: Disponibilizar o APK para download
        uses: actions/upload-artifact@v3
        with:
          name: jarvis-core-v8-debug
          path: android/app/build/outputs/apk/debug/app-debug.apk
