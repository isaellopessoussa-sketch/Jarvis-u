{
  "name": "jarvis-app",
  "version": "1.0.0",
  "description": "Meu Assistente Jarvis",
  "main": "index.html",
  "scripts": {
    "build": "mkdir -p www && cp index.html www/ && cp -r assets www/ 2>/dev/null || true"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/android": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
